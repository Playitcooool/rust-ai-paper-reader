{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability for the Paper Reader app.",
  "windows": ["main"],
  "permissions": ["core:default", "shell:allow-open"]
}
